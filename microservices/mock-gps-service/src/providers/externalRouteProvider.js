async function getExternalRoute() {
    // Integration placeholder:
    // Replace this with your preferred real routing provider (Google, Here, Mapbox, TomTom, etc.).
    // Return an array of points in { latitude, longitude } format.
    return null;
}

module.exports = {
    getExternalRoute
};
