apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: rks-logistics
spec:
  selector:
    app: ml-service
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
