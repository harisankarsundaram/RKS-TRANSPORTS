apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: rks-logistics
spec:
  selector:
    app: auth-service
  ports:
    - port: 3201
      targetPort: 3201
      protocol: TCP
  type: ClusterIP
