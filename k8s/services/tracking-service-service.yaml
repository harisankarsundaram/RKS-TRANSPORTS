apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  namespace: rks-logistics
spec:
  selector:
    app: tracking-service
  ports:
    - port: 3205
      targetPort: 3205
      protocol: TCP
  type: ClusterIP
