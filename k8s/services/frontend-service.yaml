apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: rks-logistics
spec:
  selector:
    app: rks-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30080
  type: NodePort
