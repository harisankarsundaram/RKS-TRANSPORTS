apiVersion: v1
kind: Service
metadata:
  name: trip-service
  namespace: rks-logistics
spec:
  selector:
    app: trip-service
  ports:
    - port: 3203
      targetPort: 3203
      protocol: TCP
  type: ClusterIP
