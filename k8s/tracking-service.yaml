apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-service
  namespace: rks-logistics
  labels:
    app: tracking-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tracking-service
  template:
    metadata:
      labels:
        app: tracking-service
    spec:
      containers:
        - name: tracking-service
          image: your-registry/rks-tracking-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3205
          envFrom:
            - configMapRef:
                name: rks-shared-config
            - secretRef:
                name: rks-shared-secrets
          env:
            - name: PORT
              value: "3205"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: rks-shared-secrets
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 3205
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3205
            initialDelaySeconds: 25
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  namespace: rks-logistics
spec:
  type: ClusterIP
  selector:
    app: tracking-service
  ports:
    - name: http
      port: 3205
      targetPort: 3205
      protocol: TCP
