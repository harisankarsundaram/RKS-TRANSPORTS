apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-gps-service
  namespace: rks-logistics
  labels:
    app: mock-gps-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mock-gps-service
  template:
    metadata:
      labels:
        app: mock-gps-service
    spec:
      containers:
        - name: mock-gps-service
          image: your-registry/rks-mock-gps-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3206
          envFrom:
            - configMapRef:
                name: rks-shared-config
            - secretRef:
                name: rks-shared-secrets
          env:
            - name: PORT
              value: "3206"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: rks-shared-secrets
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 3206
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3206
            initialDelaySeconds: 25
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: mock-gps-service
  namespace: rks-logistics
spec:
  type: ClusterIP
  selector:
    app: mock-gps-service
  ports:
    - name: http
      port: 3206
      targetPort: 3206
      protocol: TCP
