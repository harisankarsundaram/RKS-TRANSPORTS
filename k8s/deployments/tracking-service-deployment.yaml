apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-service
  namespace: rks-logistics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tracking-service
  template:
    metadata:
      labels:
        app: tracking-service
    spec:
      containers:
        - name: tracking-service
          image: docker.io/harisankarsundaram/rks-tracking-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3205
          envFrom:
            - configMapRef:
                name: rks-shared-config
            - secretRef:
                name: rks-shared-secrets
          env:
            - name: PORT
              value: "3205"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: rks-shared-secrets
                  key: DATABASE_URL
