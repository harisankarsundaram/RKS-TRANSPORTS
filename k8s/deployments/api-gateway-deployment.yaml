apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: rks-logistics
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: DOCKERHUB_USERNAME/rks-api-gateway:latest
          ports:
            - containerPort: 3200
          envFrom:
            - configMapRef:
                name: rks-shared-config
            - secretRef:
                name: rks-shared-secrets
          env:
            - name: PORT
              value: "3200"
