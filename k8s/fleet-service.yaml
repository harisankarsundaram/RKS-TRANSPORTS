apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleet-service
  namespace: rks-logistics
  labels:
    app: fleet-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fleet-service
  template:
    metadata:
      labels:
        app: fleet-service
    spec:
      containers:
        - name: fleet-service
          image: your-registry/rks-fleet-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3202
          envFrom:
            - configMapRef:
                name: rks-shared-config
            - secretRef:
                name: rks-shared-secrets
          env:
            - name: PORT
              value: "3202"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: rks-shared-secrets
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 3202
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3202
            initialDelaySeconds: 25
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: fleet-service
  namespace: rks-logistics
spec:
  type: ClusterIP
  selector:
    app: fleet-service
  ports:
    - name: http
      port: 3202
      targetPort: 3202
      protocol: TCP
