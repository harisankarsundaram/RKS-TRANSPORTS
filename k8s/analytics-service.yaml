apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: rks-logistics
  labels:
    app: analytics-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
        - name: analytics-service
          image: your-registry/rks-analytics-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3207
          envFrom:
            - configMapRef:
                name: rks-shared-config
            - secretRef:
                name: rks-shared-secrets
          env:
            - name: PORT
              value: "3207"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: rks-shared-secrets
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 3207
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3207
            initialDelaySeconds: 25
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: rks-logistics
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
    - name: http
      port: 3207
      targetPort: 3207
      protocol: TCP
