apiVersion: apps/v1
kind: Deployment
metadata:
  name: optimization-service
  namespace: rks-logistics
  labels:
    app: optimization-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: optimization-service
  template:
    metadata:
      labels:
        app: optimization-service
    spec:
      containers:
        - name: optimization-service
          image: your-registry/rks-optimization-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3209
          envFrom:
            - configMapRef:
                name: rks-shared-config
            - secretRef:
                name: rks-shared-secrets
          env:
            - name: PORT
              value: "3209"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: rks-shared-secrets
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 3209
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3209
            initialDelaySeconds: 25
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: optimization-service
  namespace: rks-logistics
spec:
  type: ClusterIP
  selector:
    app: optimization-service
  ports:
    - name: http
      port: 3209
      targetPort: 3209
      protocol: TCP
