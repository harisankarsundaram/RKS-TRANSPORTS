apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: rks-logistics
  labels:
    app: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: your-registry/rks-auth-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3201
          envFrom:
            - configMapRef:
                name: rks-shared-config
            - secretRef:
                name: rks-shared-secrets
          env:
            - name: PORT
              value: "3201"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: rks-shared-secrets
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 3201
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3201
            initialDelaySeconds: 25
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: rks-logistics
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 3201
      targetPort: 3201
      protocol: TCP
