apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-service
  namespace: rks-logistics
  labels:
    app: alert-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alert-service
  template:
    metadata:
      labels:
        app: alert-service
    spec:
      containers:
        - name: alert-service
          image: your-registry/rks-alert-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3208
          envFrom:
            - configMapRef:
                name: rks-shared-config
            - secretRef:
                name: rks-shared-secrets
          env:
            - name: PORT
              value: "3208"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: rks-shared-secrets
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 3208
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3208
            initialDelaySeconds: 25
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: alert-service
  namespace: rks-logistics
spec:
  type: ClusterIP
  selector:
    app: alert-service
  ports:
    - name: http
      port: 3208
      targetPort: 3208
      protocol: TCP
